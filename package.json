{
  "name": "consensus-cli",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile consensus",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.11"
  }
}
