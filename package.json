{
  "name": "consensus-cli",
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile consensus",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.11"
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
